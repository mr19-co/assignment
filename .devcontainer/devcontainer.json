{
    "name": "assignments",
    "dockerFile": "./Dockerfile",
    "remoteUser": "${localEnv:USER}",
    "build": {
        "args": {
            "USER": "${localEnv:USER}"
        }
    },
    "customizations": {
        "vscode": {
            "extensions":[
                "ms-vscode.cmake-tools"
            ]
        }
    },
    "containerEnv": {
        "TZ": "Europe/Rome"
    }
}
